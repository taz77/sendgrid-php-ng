pipeline:
  build:
    image: debian
    commands:
      - sleep 1
      - apt-get update
      - apt-get upgrade -y
      - echo "This is the build step.."
      - echo "This is a second build step of build..."
      - ls -alh

